# python-automation

## プロジェクト概要

Python による自動化スクリプトの管理リポジトリ。

---

## Git 運用ルール

### コード変更のたびに GitHub へプッシュする

コードを変更（追加・修正・削除）するたびに、以下の手順で必ず GitHub にプッシュすること。

```bash
git add <変更ファイル>
git commit -m "コミットメッセージ"
git push origin <ブランチ名>
```

- `git add .` や `git add -A` は使わず、変更ファイルを明示的に指定すること
- 認証情報・APIキー・トークンを含むファイルは絶対にコミットしないこと
- シークレットは `.env` ファイルや環境変数で管理し、`.gitignore` で除外すること
- コミットメッセージは変更内容が伝わる簡潔な日本語または英語で書くこと

### ブランチ運用

- メインブランチ: `main`
- 機能追加・バグ修正は原則フィーチャーブランチで作業し、PR を通してマージする
- `main` への直接プッシュは緊急時のみ

### コミットメッセージの形式

```
<種別>: <概要>

例:
feat: スプレッドシート自動集計スクリプトを追加
fix: スケジューラーのバグを修正
refactor: 関数を整理
docs: README を更新
```

---

## ファイルパスの記載

ファイルやコードに言及するときは、必ずファイルパスを以下の形式で記載すること。

ファイル：scripts/example.py

---

## コーディング方針

- コメントは原則書かない。WHY が非自明な場合のみ一行で記述する
- エラーハンドリングはシステム境界（ユーザー入力・外部 API）のみに限定する
- 不要な抽象化・将来の要件への備えは行わない
- 認証情報・シークレットは環境変数または `.env` ファイルで管理し、コードにハードコードしない
- セキュリティ上の脆弱性（コマンドインジェクション等）を導入しないよう注意する

---

## 大量タスクの処理

タスクが大量・大規模になる場合は、サブエージェント（Agentツール）を活用して並列・分担処理すること。
